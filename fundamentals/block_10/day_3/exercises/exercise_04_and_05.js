const capitalize = (string) => string.toUpperCase();

const firstLetter = (string) => string[0];

const concatenateStrings = (string1, string2) => string1 + string2;

module.exports = { capitalize, firstLetter, concatenateStrings }